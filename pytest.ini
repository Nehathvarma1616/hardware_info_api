[pytest]
pythonpath = .
filterwarnings =
    ignore:.*BlockingPortal alias is deprecated:DeprecationWarning

; filterwarnings =
;     error
;     ignore::UserWarning
;     ignore:function ham\(\) is deprecated:DeprecationWarning
; from pytest docs

; if want to hide warning summary completely 
; pytest --disable-warnings
